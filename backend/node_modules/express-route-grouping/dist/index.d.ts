import { IRouter as IExpressRouter } from 'express';
import { ResourceOptions } from './types';
declare type GroupCallback = (router: IRouter) => void;
declare type IRouter = IExpressRouter & {
    group: (path: string, fn: GroupCallback) => void;
    resource: (options: ResourceOptions) => void;
    export: () => IExpressRouter;
};
declare class RouteGroup {
    private head;
    private router;
    private proxyRouter?;
    constructor(path?: string, router?: IExpressRouter);
    group: (path: string | undefined, fn: GroupCallback) => void;
    resource: (options: ResourceOptions) => void;
    export: () => IExpressRouter;
    to: (suffix?: string) => string;
    private sanitize;
    private toCamelCase;
    private callRouter;
    private getPlaceholder;
    private createProxy;
}
export default RouteGroup;
